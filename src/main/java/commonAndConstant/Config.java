package commonAndConstant;

public class Config {
    /**Clean browser cookies after each iteration (if true - cookies will be deleted) */
    public static final Boolean CLEAR_COOKIES = false;
    /** To keep the browser open after all tests (if true - browser won't be closed */
    public static final Boolean HOLD_BROWSER_OPEN = false;
    /** Clear screenshots in directory everytime when launching tests (if true - screen reports will be deleted */
    public static final Boolean CLEAR_REPORTS_DIRECTORY = true;
}
